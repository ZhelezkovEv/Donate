// Добавляем вспомогательную функцию для форматирования денежных сумм в долларах:

export function formatAmount(amount) {
    return `${amount}$`;
  }

  // Эта функция принимает число и возвращает его форматированное представление в долларах 